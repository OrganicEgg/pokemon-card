# pokemon-card
Display Pokemon Information using Typescript

(No compiler needed) 

# Usage (Development)

## Typescript:
`tsc --watch`
## Local Server:
`cd dist/`
<br/>
`http-server --port 3000 -c-1 --watch`
